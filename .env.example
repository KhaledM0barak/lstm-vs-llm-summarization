# Copy to .env and fill in. .env is gitignored and must never be committed.
# Only required for the LLM baseline (src/llm/baseline.py); every other step
# runs offline once the dataset is downloaded.
ANTHROPIC_API_KEY=sk-ant-...
