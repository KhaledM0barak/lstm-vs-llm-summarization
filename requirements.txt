# Pinned to the exact versions used to produce the reported results.
# Python 3.13.1, macOS 15 (Apple silicon). See README for setup.

# Model
torch==2.13.0

# Data
datasets==5.0.1
pyarrow==25.0.0
numpy==2.5.1
pandas==3.0.5

# Evaluation
rouge-score==0.1.2
absl-py==2.5.0          # required by rouge-score
nltk==3.10.2            # required by rouge-score
scipy==1.18.0

# LLM baseline -- local open-weights model on Apple silicon (primary path).
# The markers keep `pip install -r requirements.txt` working on other hardware,
# where there is no mlx wheel: use --backend anthropic to generate, or
# `python -m src.demo --replay-llm` to show the recorded responses.
mlx==0.32.0; platform_machine == "arm64" and sys_platform == "darwin"
mlx-lm==0.31.3; platform_machine == "arm64" and sys_platform == "darwin"

# LLM baseline -- hosted API (alternative path, --backend anthropic)
anthropic==0.121.0
python-dotenv==1.2.2

# Report rendering (markdown -> HTML -> PDF via headless Chrome)
markdown==3.10.3

# Tests
pytest==9.1.1

# Plumbing
PyYAML==6.0.3
tqdm==4.70.0
matplotlib==3.11.1
